package com.teamteorganiza.infra;

public class SupabaseConfigExample {
    // Cole aqui a connection string do Supabase (Settings → Database → JDBC)
    // com a senha URL-encodada: * → %2A   + → %2B   @ → %40   # → %23
    public static final String CONNECTION_URL =
        "exemplo1" +
        "exemplo2";
}
